# This achievement is awarded for completing a problem with no
# incorrect submissions.
#
# See [templates]/achievements/achievement_readme.txt for details.

return $problem->status == 1 && $problem->num_incorrect == 0 ? 1 : 0;
