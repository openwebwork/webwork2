# Award this achievement if a student solves a problem after
# answering it incorrectly more than 100 times.
#
# See [templates]/achievements/achievement_readme.txt for details.

if ($problem->status == 1
	&& $problem->num_correct == 1
	&& $problem->num_incorrect >= 100)
{
	return 1;
}

return 0;
