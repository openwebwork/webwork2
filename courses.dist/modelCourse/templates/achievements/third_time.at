# This achievement is awarded for completing a problem correctly on the
# third submit.
#
# See [templates]/achievements/achievement_readme.txt for details.

if ($problem->num_correct == 1
	&& $problem->num_incorrect == 2
	&& $problem->status == 1)
{
	return 1;
}

return 0;
