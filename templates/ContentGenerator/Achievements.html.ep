% use WeBWorK::Utils qw(getAssetURL);
%
% content_for css => begin
	<%= stylesheet $cg->url({ type => 'webwork', name => 'theme', file => 'achievements.css' }) =%>
% end
%
% content_for js => begin
	<%= javascript getAssetURL($ce, 'js/apps/AchievementItems/achievementitems.js'), defer => undef =%>
% end
%
% # Exit if there is no global achievement data for this user.
% unless (defined $cg->{globalData}) {
	<p><%= maketext(q{You don't have any Achievement data associated to you!}) =%></p>
	% last;
% }
%
<%= include 'ContentGenerator/Achievements/cheevobigbox', $cg->getAchievementLevelData =%>
<%= include 'ContentGenerator/Achievements/achievement_items', $cg->getAchievementItemsData =%>
<%= include 'ContentGenerator/Achievements/achievement_badges', $cg->getAchievementsData =%>
